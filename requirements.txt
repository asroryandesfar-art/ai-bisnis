# BotNesia Multi-Agent — Dependencies
# pip install -r requirements.txt

fastapi==0.111.1
uvicorn[standard]==0.50.2
asyncpg==0.29.0
# M-05: python-jose 3.3.0 ber-CVE (algorithm-confusion / DoS dekode JWE).
# Pin ke 3.5.0 (versi yang terpasang & teruji, sudah di atas perbaikan CVE).
python-jose[cryptography]==3.5.0
passlib==1.7.4
httpx==0.28.1
pydantic==2.7.4
pydantic-settings==2.3.4
# python-multipart: <0.0.18 DoS (M-05); 0.0.30 juga kena CVE-2026-53540 -> 0.0.31.
python-multipart==0.0.32
pypdf==6.10.2

# --- Web Intelligence (backend/modules/web_intelligence) — optional but
# recommended for best content extraction. Module degrades gracefully to a bs4
# heuristic if these are absent (see /api/web-intelligence/status). ---
trafilatura==2.1.0
readability-lxml==0.8.4.1

# --- Intelligence Platform (Conversation Memory, FAQ, Sales, Knowledge Graph) ---
numpy==2.4.4
celery[redis]==5.4.0
redis==5.0.7

# --- Business Platform Phase 2 (RBAC, Billing, Omnichannel, Security, Observability) ---
cryptography==43.0.1
prometheus-client==0.25.0

# --- Phase 3 Multimodal (Document Generation) ---
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==5.0.0

# --- Investor-Readiness Assets (marketing screenshot capture, dev-only) ---
# Setelah pip install, jalankan sekali: python3 -m playwright install chromium
playwright==1.60.0

# --- Casper Blockchain (Casper Agentic Buildathon 2026) ---
pycspr==1.2.0

# --- Knowledge Base local semantic embedding (gratis, tanpa API key) ---
# Download model (~470MB) otomatis saat panggilan pertama, sekali saja (cached).
sentence-transformers==5.6.0
